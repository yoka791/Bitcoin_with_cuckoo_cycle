./autogen.sh
./configure
make install

cd /usr/local/bin
./bitcoind -cuckoo -port=8330 -rpcport=8331 -rpcuser=user -rpcpassword=password -datadir=/root/node1data -printtoconsole

cd /usr/local/bin
./bitcoind -cuckoo -port=8333 -rpcport=8332 -rpcuser=user2 -rpcpassword=password2 -datadir=/root/node2data -printtoconsole -connect=127.0.0.1:8330

cd /usr/local/bin
./bitcoin-cli -cuckoo -rpcport=8332 -rpcuser=user2 -rpcpassword=password2  getblockcount

./bitcoin-cli -cuckoo -rpcport=8331 -rpcuser=user -rpcpassword=password  setgenerate true 1
./bitcoin-cli -cuckoo -rpcport=8332 -rpcuser=user2 -rpcpassword=password2  setgenerate true 1

./bitcoin-cli -cuckoo -rpcport=8331 -rpcuser=user -rpcpassword=password  setgenerate false
./bitcoin-cli -cuckoo -rpcport=8332 -rpcuser=user2 -rpcpassword=password2  setgenerate false

./bitcoin-cli -cuckoo -rpcport=8331 -rpcuser=user -rpcpassword=password  getblockcount
./bitcoin-cli -cuckoo -rpcport=8332 -rpcuser=user2 -rpcpassword=password2  getblockcount

./bitcoin-cli -cuckoo -rpcport=8331 -rpcuser=user -rpcpassword=password  getmininginfo
./bitcoin-cli -cuckoo -rpcport=8332 -rpcuser=user2 -rpcpassword=password2  getmininginfo

./bitcoin-cli -cuckoo -rpcport=8331 -rpcuser=user -rpcpassword=password  getwalletinfo
./bitcoin-cli -cuckoo -rpcport=8332 -rpcuser=user2 -rpcpassword=password2  getwalletinfo



cd ~
rm -rf .bitcoin/
rm -rf node1data/
rm -rf node2data/
mkdir node1data
mkdir node2data

cd cuc_bitcoin_0.12.1
make install
